[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "setuptools_scm[toml]>=6.2",
    "pybind11>=2.6.2"
]
build-backend = "setuptools.build_meta"

# See also src/tiledbsc/__init__.py
version = "0.1.0"
