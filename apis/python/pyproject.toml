[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "setuptools_scm[toml]>=6.2",
    "pybind11>=2.6.2"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "version.py"
