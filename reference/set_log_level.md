# Set TileDB-SOMA Logging Level

Set the logging level for the R package and underlying C++ library

## Usage

``` r
set_log_level(level)
```

## Arguments

- level:

  A character value with logging level. May be “trace”, “debug”, “info”,
  or “warn”

## Value

Invisibly returns `NULL`
